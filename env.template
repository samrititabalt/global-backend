
# ============================================================================
# OAUTH AUTHENTICATION - GOOGLE
# ============================================================================
# STEP 1: Go to https://console.cloud.google.com/
# STEP 2: Create a project or select existing one
# STEP 3: Enable "Google+ API" or "Google Identity Services"
# STEP 4: Go to "Credentials" → "Create Credentials" → "OAuth 2.0 Client ID"
# STEP 5: Set Application type: Web application
# STEP 6: Add Authorized redirect URI: {BACKEND_URL}/api/auth/google/callback
#         Example: http://localhost:5000/api/auth/google/callback (for local)
#                  https://your-backend.com/api/auth/google/callback (for production)
# STEP 7: Copy the Client ID and Client Secret below
GOOGLE_CLIENT_ID=your-google-client-id-here
GOOGLE_CLIENT_SECRET=your-google-client-secret-here

# ============================================================================
# OAUTH AUTHENTICATION - MICROSOFT
# ============================================================================
# STEP 1: Go to https://portal.azure.com/
# STEP 2: Navigate to "Azure Active Directory" → "App registrations"
# STEP 3: Click "New registration"
# STEP 4: Set redirect URI: {BACKEND_URL}/api/auth/microsoft/callback
# STEP 5: Go to "Certificates & secrets" → Create new client secret
# STEP 6: Copy the Application (client) ID and Client secret below
MICROSOFT_CLIENT_ID=your-microsoft-client-id-here
MICROSOFT_CLIENT_SECRET=your-microsoft-client-secret-here

# ============================================================================
# SESSION CONFIGURATION (for OAuth)
# ============================================================================
# Generate a random secret key for session encryption
# Run this command to generate one: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# Or use any random string (at least 32 characters long)
SESSION_SECRET=change-this-to-a-random-secret-key-at-least-32-characters-long

# ============================================================================
# NODE ENVIRONMENT
# ============================================================================
# Set to 'production' when deploying, 'development' for local
NODE_ENV=development

